package com.nttdata.bankaccountmanagementsystem.account;

/**
 * Enum that represents the different types of bank accounts.
 * <p>
 * This enum defines two types of bank accounts: SAVINGS and CHECKING.
 * Each account type may have specific rules or behaviors associated with it,
 * such as withdrawal limits or overdraft capabilities.
 * </p>
 */
public enum AccountType {
    SAVINGS,
    CHECKING
}
